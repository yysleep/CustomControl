<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sleep.yy.customcontrol">

    <application
        android:name=".CustomControlApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".main.MainActivity"
            android:launchMode="singleTask"
            android:protectionLevel="signature">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".circular.CircularProgressActivity" />
        <activity android:name=".pie.PieChartActivity" />
        <activity android:name=".geometry.GeometryActivity" />
        <activity android:name=".camera.CameraActivity" />
        <activity android:name=".weather.WeatherActivity" />
        <activity android:name=".gesture.GestureActivity"/>
        <activity android:name=".ruler.RulerActivity"/>
    </application>

</manifest>